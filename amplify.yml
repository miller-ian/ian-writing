version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --frozen-lockfile
        - 'node -v'
        - 'yarn -v'
    build:
      commands:
        - yarn build
        - 'ls -la'
        - 'ls -la out'
    postBuild:
      commands:
        - 'echo "Build completed"'
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/* 